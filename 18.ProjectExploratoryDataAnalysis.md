## 🔹 Exploratory Data Analysis (EDA)

---

## 📌 View Cleaned Data

```sql id="eda1"
-- View full dataset after cleaning
SELECT *
FROM layoffs_staging2;
```

---

## 🔹 Check Maximum & Minimum Values

```sql id="eda2"
-- Maximum layoffs and percentage
SELECT 
    MAX(total_laid_off) AS max_laid_off, 
    MAX(percentage_laid_off) AS max_percentage
FROM layoffs_staging2;
```

👉 `percentage_laid_off = 1` means **100% employees laid off**

---

```sql id="eda3"
-- Minimum layoffs and percentage
SELECT 
    MIN(total_laid_off) AS min_laid_off, 
    MIN(percentage_laid_off) AS min_percentage
FROM layoffs_staging2;
```

---

## 🔹 Companies with 100% Layoffs

```sql id="eda4"
-- Companies that laid off all employees
SELECT *
FROM layoffs_staging2
WHERE percentage_laid_off = 1
ORDER BY total_laid_off DESC;
```

---

```sql id="eda5"
-- Sort by funding to see which companies had more funding
SELECT *
FROM layoffs_staging2
WHERE percentage_laid_off = 1
ORDER BY funds_raised_millions DESC;
```

---

## 🔹 Total Layoffs by Company

```sql id="eda6"
-- Total layoffs per company
SELECT company, SUM(total_laid_off) AS total_laid_off
FROM layoffs_staging2
GROUP BY company
ORDER BY total_laid_off DESC;
```

---

## 🔹 Date Range

```sql id="eda7"
-- Find earliest and latest date
SELECT 
    MIN(`date`) AS start_date, 
    MAX(`date`) AS end_date
FROM layoffs_staging2;
```

👉 Data spans across multiple years

---

## 🔹 Layoffs by Industry

```sql id="eda8"
-- Total layoffs per industry
SELECT 
    industry, 
    SUM(total_laid_off) AS total_laid_off
FROM layoffs_staging2
GROUP BY industry
ORDER BY total_laid_off DESC;
```

---

## 🔹 Layoffs by Country

```sql id="eda9"
-- Total layoffs per country
SELECT 
    country, 
    SUM(total_laid_off) AS total_laid_off
FROM layoffs_staging2
GROUP BY country
ORDER BY total_laid_off DESC;
```

---

## 🔹 Layoffs Over Time (Daily)

```sql id="eda10"
-- Total layoffs per day
SELECT `date`, SUM(total_laid_off) AS total_laid_off
FROM layoffs_staging2
GROUP BY `date`
ORDER BY `date` DESC;
```

---

## 🔹 Layoffs by Year

```sql id="eda11"
-- Total layoffs per year
SELECT 
    YEAR(`date`) AS year, 
    SUM(total_laid_off) AS total_laid_off
FROM layoffs_staging2
GROUP BY YEAR(`date`)
ORDER BY year DESC;
```

👉 Helps identify trends over time

---

## 🔹 Layoffs by Stage

```sql id="eda12"
-- Total layoffs by company stage
SELECT 
    stage, 
    SUM(total_laid_off) AS total_laid_off
FROM layoffs_staging2
GROUP BY stage
ORDER BY total_laid_off DESC;
```

---

## 🔹 Layoffs by Location

```sql id="eda13"
-- Total layoffs by location
SELECT 
    location, 
    SUM(total_laid_off) AS total_laid_off
FROM layoffs_staging2
GROUP BY location
ORDER BY total_laid_off DESC;
```

---

## 🔹 Monthly Layoffs Trend

```sql id="eda14"
-- Monthly aggregation using DATE_FORMAT
SELECT 
    DATE_FORMAT(`date`, '%Y-%m') AS month,
    SUM(total_laid_off) AS total_laid_off
FROM layoffs_staging2
GROUP BY month
ORDER BY month DESC;
```

---

```sql id="eda15"
-- Alternative method using SUBSTRING
SELECT 
    SUBSTRING(`date`, 1, 7) AS month,
    SUM(total_laid_off) AS total_laid_off
FROM layoffs_staging2
WHERE SUBSTRING(`date`, 1, 7) IS NOT NULL
GROUP BY month
ORDER BY month;
```

---

## 🔹 Rolling Total (Cumulative Layoffs)

```sql id="eda16"
-- Calculate cumulative layoffs over time
WITH rolling_total AS (
    SELECT 
        SUBSTRING(`date`, 1, 7) AS month,
        SUM(total_laid_off) AS monthly_total
    FROM layoffs_staging2
    WHERE SUBSTRING(`date`, 1, 7) IS NOT NULL
    GROUP BY month
)
SELECT 
    month,
    monthly_total,
    SUM(monthly_total) OVER (ORDER BY month) AS rolling_total
FROM rolling_total;
```

---

## 🔹 Layoffs by Company per Year

```sql id="eda17"
-- Total layoffs per company per year
SELECT 
    company, 
    YEAR(`date`) AS year, 
    SUM(total_laid_off) AS total_laid_off
FROM layoffs_staging2
GROUP BY company, YEAR(`date`)
ORDER BY company ASC;
```

---

```sql id="eda18"
-- Sort by highest layoffs
SELECT 
    company, 
    YEAR(`date`) AS year, 
    SUM(total_laid_off) AS total_laid_off
FROM layoffs_staging2
GROUP BY company, YEAR(`date`)
ORDER BY total_laid_off DESC;
```

---

## 🔹 Top Companies by Layoffs Each Year

```sql id="eda19"
-- Create base data
WITH company_year AS (
    SELECT 
        company, 
        YEAR(`date`) AS year, 
        SUM(total_laid_off) AS total_laid_off
    FROM layoffs_staging2
    GROUP BY company, YEAR(`date`)
),
company_year_rank AS (
    -- Rank companies within each year
    SELECT *,
        DENSE_RANK() OVER (
            PARTITION BY year 
            ORDER BY total_laid_off DESC
        ) AS ranking
    FROM company_year
    WHERE year IS NOT NULL
)
-- Get top 5 companies per year
SELECT *
FROM company_year_rank
WHERE ranking <= 5;
```

---

## 🧠 Key Insights

* Identify companies with highest layoffs
* Track layoffs trend over time (daily, monthly, yearly)
* Compare layoffs by industry, country, and stage
* Detect patterns using rolling totals
* Rank top companies by layoffs per year

---

## 🎯 Simple Understanding

EDA helps you:

* understand the dataset
* find trends and patterns
* prepare data for visualization and insights

---
