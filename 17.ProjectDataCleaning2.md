## 🔹 Handling NULL Values & Final Data Cleaning

---

## 📌 View Cleaned Table

```sql id="n1"
SELECT *
FROM layoffs_staging2;
```

---

## 📌 Check Rows with Missing Layoff Data

```sql id="n2"
-- Rows where both total_laid_off and percentage_laid_off are NULL
SELECT *
FROM layoffs_staging2
WHERE total_laid_off IS NULL
  AND percentage_laid_off IS NULL;
```

---

## 📌 Check NULL Company Values

```sql id="n3"
SELECT company
FROM layoffs_staging2
WHERE company IS NULL;
```

---

## 📌 Check Industry Values

```sql id="n4"
SELECT DISTINCT industry
FROM layoffs_staging2;
```

```sql id="n5"
-- Find rows where industry is NULL or blank
SELECT *
FROM layoffs_staging2
WHERE industry IS NULL
   OR industry = '';
```

---

## 📌 Example Check (Specific Company)

```sql id="n6"
SELECT *
FROM layoffs_staging2
WHERE company = 'Airbnb';
```

---

## 🔹 Fill Missing Industry using Self JOIN

```sql id="n7"
-- Match same company to get industry from other rows
SELECT t1.industry, t2.industry
FROM layoffs_staging2 t1
JOIN layoffs_staging2 t2
  ON t1.company = t2.company
WHERE (t1.industry IS NULL OR t1.industry = '')
  AND t2.industry IS NOT NULL;
```

---

```sql id="n8"
-- Update missing industry values
UPDATE layoffs_staging2 t1
JOIN layoffs_staging2 t2
  ON t1.company = t2.company
SET t1.industry = t2.industry
WHERE (t1.industry IS NULL OR t1.industry = '')
  AND t2.industry IS NOT NULL
  AND t2.industry <> '';
```

---

## 📌 Verify Industry Data

```sql id="n9"
SELECT industry, COUNT(*)
FROM layoffs_staging2
GROUP BY industry;
```

---

## 📌 Additional Checks

```sql id="n10"
SELECT *
FROM layoffs_staging2
WHERE company LIKE 'Bally%';
```

---

```sql id="n11"
SELECT *
FROM layoffs_staging2
WHERE total_laid_off IS NULL
  AND percentage_laid_off IS NULL;
```

---

## 🔹 Remove Unnecessary Rows

```sql id="n12"
-- Delete rows where both layoff columns are NULL
DELETE
FROM layoffs_staging2
WHERE total_laid_off IS NULL
  AND percentage_laid_off IS NULL;
```

---

## 🔹 Remove Helper Column

```sql id="n13"
-- Drop row_num column (used for duplicate removal)
ALTER TABLE layoffs_staging2
DROP COLUMN row_num;
```

---

## 📌 Final Cleaned Data

```sql id="n14"
SELECT *
FROM layoffs_staging2;
```

---

## 🧠 Final Summary

* Removed duplicate rows
* Filled missing industry values using self join
* Deleted rows with no useful layoff data
* Standardized company, location, industry, and date values
* Removed helper column (`row_num`) after cleaning

---
